package kenming.pattern.structure.bridge.dto;

public class Types {
    // Notifier 的類型
    public enum NotifyType
    { User, System }

    // MessageSender 的類型
    public enum SenderType
    { Email, Line}
}
